dsn: postgres://gk_circle:change_me_database_password@db:5432/kratos?sslmode=disable

serve:
  public:
    base_url: http://localhost:4433/
    port: 4433
    cors:
      enabled: true
      allowed_origins:
        - __CORS_ALLOWED_ORIGINS__
      allowed_methods:
        - POST
        - GET
        - PUT
        - PATCH
        - DELETE
      allowed_headers:
        - Authorization
        - Cookie
        - Content-Type
      exposed_headers:
        - Content-Type
        - Set-Cookie
      allow_credentials: true # Ensure this line is included and set to true
  admin:
    base_url: http://localhost:4434/
    port: 4434

selfservice:
  default_browser_return_url: __DEFAULT_BROWSER_RETURN_URL__
  allowed_return_urls:
    - __SELFSERVICE_ALLOWED_RETURN_URLS__

  methods:
    password:
      enabled: true
    oidc:
      enabled: true
      config:
        base_redirect_uri: http://localhost:4433
        providers:
          - id: google
            provider: google
            mapper_url: "base64://<BASE64-JASONNATE_SCHEMA>"
            auth_url: https://accounts.google.com/o/oauth2/v2/auth
            token_url: https://www.googleapis.com/oauth2/v4/token
            issuer_url: https://accounts.google.com
            client_id: <GOOGLE_CLIENT_ID>
            client_secret: <GOOGLE_CLIENT_SECRET>
            scope:
              - openid
              - email
              - profile
    totp:
      config:
        issuer: Kratos
      enabled: true
    lookup_secret:
      enabled: true
    link:
      enabled: true
    code:
      enabled: true

  flows:
    error:
      ui_url: __SELFSERVICE_FLOWS_ERROR_UI_URL__

    settings:
      ui_url: __SELFSERVICE_FLOWS_SETTINGS_UI_URL__
      privileged_session_max_age: 15m
      required_aal: highest_available

    recovery:
      enabled: true
      ui_url: __SELFSERVICE_FLOWS_RECOVERY_UI_URL__
      use: code

    verification:
      enabled: true
      ui_url: __SELFSERVICE_FLOWS_VERIFICATION_UI_URL__
      use: code
      after:
        default_browser_return_url: __SELFSERVICE_FLOWS_VERIFICATION_AFTER_DEFAULT_BROWSER_RETURN_URL__

    logout:
      after:
        default_browser_return_url: __SELFSERVICE_FLOWS_LOGOUT_AFTER_DEFAULT_BROWSER_RETURN_URL__

    login:
      ui_url: __SELFSERVICE_FLOWS_LOGIN_UI_URL__
      # http://127.0.0.1:4433/self-service/login/browser?aal=&refresh=&return_to=
      after:
        default_browser_return_url: __SELFSERVICE_FLOWS_LOGIN_AFTER_DEFAULT_BROWSER_RETURN_URL__
      lifespan: 10m

    registration:
      lifespan: 10m
      ui_url: __SELFSERVICE_FLOWS_REGISTRATION_UI_URL__
      after:
        default_browser_return_url: __SELFSERVICE_FLOWS_REGISTRATION_AFTER_DEFAULT_BROWSER_RETURN_URL__
        password:
          hooks:
            - hook: session
            - hook: show_verification_ui
        oidc:
          hooks:
            - hook: session
            - hook: show_verification_ui
log:
  level: debug
  format: text
  leak_sensitive_values: false

secrets:
  cookie:
    - change_me_to_a_long_random_cookie_secret
  cipher:
    - change_me_exactly_32_char_secret

ciphers:
  algorithm: xchacha20-poly1305

hashers:
  algorithm: bcrypt
  bcrypt:
    cost: 8

identity:
  default_schema_id: default
  schemas:
    - id: default
      url: file:///etc/config/kratos/identity.schema.json

courier:
  smtp:
    connection_uri: ${COURIER_SMTP_CONNECTION_URI}
    from_name: GK Circle
    from_address: ${COURIER_SMTP_FROM_ADDRESS}

  template_override_path: /etc/config/kratos/email-templates

cookies:
  domain: __COOKIES_DOMAIN__
  path: /
  same_site: Lax
