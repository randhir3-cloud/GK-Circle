IS_DEVELOPMENT=true
DEBUG=true
APP_PORT=0.0.0.0:3000
APP_ENV=local


# Database Config
DB_DIALECT=postgres
DB_HOST=db
DB_PORT=5432
DB_USERNAME=gk_circle
DB_PASSWORD=change_me_database_password
DB_NAME=gk_circle

# for postgres used this query sting
DB_QUERYSTRING=sslmode=disable

# front-end url
WEB_URL=http://localhost:3000

# for mysql used this query string
# DB_QUERYSTRING=parseTime=true

MIGRATION_DIR=database/migrations
# SQLITE_FILEPATH=database/gk_circle.db

#JWT
JWT_SECRET=change_me_to_a_long_random_string
ISSUER=gk-circle.local

#Redis
REDIS_HOST=redis
REDIS_PASSWORD=change_me_redis_password
REDIS_PORT=6379
REDIS_DATABASES=0

# Report scheduler
REPORT_SCHEDULER_TIMEOUT_SECONDS=10

# Local transactional email (internal Compose network only)
EMAIL_PROVIDER=smtp
EMAIL_FROM_NAME=GK Circle Local
EMAIL_FROM=notifications@example.invalid
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_DISABLE_STARTTLS=true

#KRATOS
KRATOS_ENABLED=true
KRATOS_COOKIE_EXPIRATION_TIME=2h23m # Time should be in the format of 1h23m, valid units are "h", "m", "s", "ms", "us", "ns"
SERVE_PUBLIC_BASE_URL=http://kratos:4433 # URL where the endpoint is exposed at. **used to generate redirects.
SERVE_PUBLIC_HOST=kratos # host on which the public endpoint listenes on.
SERVE_PUBLIC_PORT=4433 # port of the kratos public endpoint.

# SERVE_ADMIN_BASE_URL=http://xyz.com # URL where the admin endpoint is exposed at.
# SERVE_ADMIN_HOST=127.0.0.1 # host on which the admin endpoint listenes on.
# SERVE_ADMIN_PORT=4451 # port on which the admin endpoint listens on.

# SELF_SERVICE_DEFAULT_BROWSER_RETURN_URL=http://127.0.0.1:4455
# SELFSERVICE_ALLOWED_RETURN_URLS_0=http://127.0.0.1:4455
# SELFSERVICE_ALLOWED_RETURN_URLS_1=http://127.0.0.1:4433

# # Message queue config
# MQ_TRACK=true
# MQ_DEBUG=true
# DEAD_LETTER_QUEUE=dead_queue
# HANDLER_NAME=handler

# # sql
# MQ_DIALECT=sql
# MQ_DB_DIALECT=postgres
# MQ_DB_HOST=localhost
# MQ_DB_PORT=5432
# MQ_DB_USERNAME=gk_circle
# MQ_DB_PASSWORD=change_me_database_password
# MQ_DB_NAME=gk_circle
# MQ_DB_QUERYSTRING=sslmode=disable

# Rabbitmq
# MQ_DIALECT=amqp
# AMQB_URI=amqp://guest:guest@localhost:5672/

# Redis
# MQ_DIALECT=redis
# REDIS_HOST=
# CONSUMER_GROUP=
# REDIS_USERNAME=
# REDIS_PASSWORD=

# kafka
# MQ_DIALECT=kafka
# KAFKA_BROKER=
# CONSUMER_GROUP=

# googleCloud
# MQ_DIALECT=googlecloud
# PROJECT_ID=
# SUBSCRIPTION_ID=
